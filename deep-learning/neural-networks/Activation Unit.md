- The **activation unit** is a unit that takes an input and applies an activation function to it.

$$
a = \sigma(z)
$$

where:
- $a$ is the output of the activation unit
- $\sigma$ is the activation function
